#include<bits/stdc++.h>
using namespace std;
int potegowanie(int potega, int wykladnik)
{	
	int suma = 1;
	if(wykladnik == 0) return suma;
	for(int i = 0 ; i < wykladnik; i++)
		suma *= potega;
	return suma;
}
int main()
{
	cout<<potegowanie(3,3);
}
